import './select-input.scss';

const SelectInput = (props: any) => {
  return (
    <select className="devugo-select-input" {...props}>
      <option>dkdk</option>
    </select>
  );
};

export default SelectInput;
